# Only build tools for desktop.
if(NOT LOOM_BUILD_IOS EQUAL 1)
if(NOT ANDROID)
	subdirs(lsc)
	subdirs(ldb)	
	subdirs(assetAgent)
	subdirs(loomexec)
	subdirs(unittest)
	if(MSVC)
	    subdirs(winloom)
	endif()
endif()
endif()	